---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: cloudpipelines/pipeline-base
    tag: latest

inputs:
  - name: code-repo
  - name: ci-repo
  - name: devops-repo

outputs:
  - name: devops-repo-modified

run:
  path: ci-repo/ci/tasks/init-env.sh
  args:
    - ci-repo/ci/tasks/update-gitops.sh
